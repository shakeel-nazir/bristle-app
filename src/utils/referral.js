export const REFERRAL_DISCOUNT_PERCENT = 25;

// Promo codes work on every account, any number of times (unlike referral codes, which belong to one
// person and can only be redeemed once). Add or change codes here: { CODE: percentOff }.
export const PROMO_CODES = {
  BRISTLE25: 25,
};
